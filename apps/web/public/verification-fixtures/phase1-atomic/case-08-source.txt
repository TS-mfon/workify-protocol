WORKIFY ATOMIC SOFTWARE CASE 8
FUNCTION: isValidHex

LOCKED ISSUE
Implement isValidHex(input) to accept only an optional 0x prefix followed by an even number of hexadecimal characters. Add meaningful tests for every required behavior.

DELIVERY ASSESSMENT
The implementation includes the remaining listed behaviors for isValidHex, but omits the first required behavior.

TEST ASSESSMENT
Tests cover only the implemented behaviors.

OMISSIONS
The first required behavior is intentionally missing and its corresponding test is absent.

CI
lint PASS; unit tests PASS; typecheck PASS.

CHANGE SCOPE
Only the requested function and its focused tests changed. No secrets, dependencies, authorization logic, or unrelated files changed.
