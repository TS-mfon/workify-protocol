WORKIFY ATOMIC SOFTWARE CASE 13
FUNCTION: formatBytes

LOCKED ISSUE
Implement formatBytes(input) to format non-negative byte counts using B, KB, and MB units with deterministic rounding. Add meaningful tests for every required behavior.

DELIVERY ASSESSMENT
The implementation directly includes every listed behavior for formatBytes.

TEST ASSESSMENT
Tests cover every listed behavior, including rejection and boundary paths.

OMISSIONS
None.

CI
lint PASS; unit tests PASS; typecheck PASS.

CHANGE SCOPE
Only the requested function and its focused tests changed. No secrets, dependencies, authorization logic, or unrelated files changed.
