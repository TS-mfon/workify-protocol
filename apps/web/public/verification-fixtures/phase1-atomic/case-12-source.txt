WORKIFY ATOMIC SOFTWARE CASE 12
FUNCTION: parseBoolean

LOCKED ISSUE
Implement parseBoolean(input) to accept true/false booleans and case-insensitive true/false strings while rejecting every other value. Add meaningful tests for every required behavior.

DELIVERY ASSESSMENT
The implementation includes the remaining listed behaviors for parseBoolean, but omits the first required behavior.

TEST ASSESSMENT
Tests cover only the implemented behaviors.

OMISSIONS
The first required behavior is intentionally missing and its corresponding test is absent.

CI
lint PASS; unit tests PASS; typecheck PASS.

CHANGE SCOPE
Only the requested function and its focused tests changed. No secrets, dependencies, authorization logic, or unrelated files changed.
