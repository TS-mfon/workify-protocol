export default async function Appeal({params}:{params:Promise<{jobId:string}>}){const {jobId}=await params;return <><p className="muted">Job {jobId}</p><h1 className="page-title">Open appeal</h1><form className="glass card form" style={{marginTop:28}}><div className="field"><label>Appeal statement</label><textarea rows={7} placeholder="Explain the specific criterion or evidence the original verdict misinterpreted"/></div><div className="field"><label>Supplemental public evidence URL</label><input placeholder="https://…"/></div><p className="muted">Opening an intent freezes settlement for up to 30 minutes while the exact 1 GEN appeal payment finalizes.</p><button className="button" type="button">Open appeal intent</button></form></>}
