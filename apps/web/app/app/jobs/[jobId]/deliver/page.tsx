export default async function Deliver({params}:{params:Promise<{jobId:string}>}){const {jobId}=await params;return <><p className="muted">Job {jobId}</p><h1 className="page-title">Submit public evidence</h1><form className="glass card form" style={{marginTop:28}}><div className="field"><label>Public delivery URL</label><input placeholder="https://github.com/owner/repo/pull/123"/></div><div className="field"><label>Optional revision</label><input placeholder="Commit SHA is detected automatically"/></div><button className="button" type="button">Prepare and lock evidence</button></form></>}
