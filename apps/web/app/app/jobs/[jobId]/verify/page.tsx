export default async function Verify({params}:{params:Promise<{jobId:string}>}){const {jobId}=await params;return <><p className="muted">Job {jobId}</p><h1 className="page-title">Verification</h1><div className="glass card" style={{marginTop:28}}><span className="status"><span className="pulse"/> Awaiting funded attempt</span><h2>0.1 GEN per attempt</h2><p className="muted">Evidence remains locked across a maximum of three total adjudication attempts. Workify never settles from a leader-only UNDETERMINED result.</p><button className="button" type="button">Fund and request verification</button></div></>}
