import Link from "next/link";
export default async function Job({params}:{params:Promise<{jobId:string}>}){const {jobId}=await params;return <><p className="muted">Job {jobId}</p><h1 className="page-title">Work contract</h1><div className="metrics" style={{marginTop:28}}><div className="glass metric"><span className="muted">Escrow</span><b>— USDC</b></div><div className="glass metric"><span className="muted">Status</span><b>Loading</b></div><div className="glass metric"><span className="muted">Attempts</span><b>0 / 3</b></div></div><div className="actions" style={{justifyContent:"flex-start"}}><Link className="button" href={`/app/jobs/${jobId}/deliver`}>Submit delivery</Link><Link className="button secondary" href={`/app/jobs/${jobId}/verify`}>Verification</Link><Link className="button secondary" href={`/app/jobs/${jobId}/appeal`}>Appeal</Link></div></>}
