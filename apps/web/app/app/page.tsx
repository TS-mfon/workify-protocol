import Link from "next/link";
import { Activity, ArrowUpRight, BriefcaseBusiness, CircleDollarSign, Gavel, Plus, ShieldCheck } from "lucide-react";
import { MetricCard, PageHeader, SectionHeading } from "@/components/ProtocolUI";
import { LiveJobs } from "@/components/LiveLedger";

export default function Dashboard() {
  return <><PageHeader eyebrow="Protocol console · Base Sepolia" title="Active ledger" description="Track funded work from escrow through evidence, consensus, appeal, and deterministic settlement." icon={<Activity size={22} />} status={<span className="status"><span className="pulse" /> Testnet live</span>} action={<Link className="button" href="/app/jobs/new"><Plus size={16} /> Create funded job</Link>} /><div className="metrics"><MetricCard icon={<CircleDollarSign />} label="Escrowed USDC" value="Live" hint="Read from Base escrow" accent /><MetricCard icon={<BriefcaseBusiness />} label="Active jobs" value="Onchain" hint="Filtered by connected wallet" /><MetricCard icon={<ShieldCheck />} label="Final verdicts" value="GenLayer" hint="Consensus-backed results" /><MetricCard icon={<Gavel />} label="Open appeals" value="5 min" hint="Challenge window" /></div><section className="section"><SectionHeading eyebrow="Live settlement queue" title="Recent work contracts" description="The ledger only displays jobs tied to the connected address and reads current state from Base." action={<Link className="button secondary" href="/explorer">View resolved cases <ArrowUpRight size={15} /></Link>} /><div className="glass ledger-panel"><div className="ledger-toolbar"><span><Activity size={15} /> Onchain activity</span><span className="status review"><ShieldCheck size={14} /> Evidence-first</span></div><LiveJobs /></div></section></>;
}
