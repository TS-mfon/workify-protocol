import Link from "next/link";
import { Activity, ArrowUpRight, BriefcaseBusiness, CircleDollarSign, Gavel, Plus, ShieldCheck } from "lucide-react";
import { EmptyState, MetricCard, PageHeader, SectionHeading } from "@/components/ProtocolUI";

export default function Dashboard() {
  return <><PageHeader eyebrow="Protocol console · Base Sepolia" title="Active ledger" description="Track funded work from escrow through evidence, consensus, appeal, and deterministic settlement." icon={<Activity size={22} />} status={<span className="status"><span className="pulse" /> Testnet live</span>} action={<Link className="button" href="/app/jobs/new"><Plus size={16} /> Create funded job</Link>} /><div className="metrics"><MetricCard icon={<CircleDollarSign />} label="Escrowed USDC" value="—" hint="Connect wallet to read" accent /><MetricCard icon={<BriefcaseBusiness />} label="Active jobs" value="0" hint="Funded work contracts" /><MetricCard icon={<ShieldCheck />} label="Final verdicts" value="0" hint="Consensus-backed results" /><MetricCard icon={<Gavel />} label="Open appeals" value="0" hint="Five-minute challenge window" /></div><section className="section"><SectionHeading eyebrow="Live settlement queue" title="Recent work contracts" description="The ledger only displays jobs tied to the connected address." action={<Link className="button secondary" href="/explorer">View resolved cases <ArrowUpRight size={15} /></Link>} /><div className="glass ledger-panel"><div className="ledger-toolbar"><span><Activity size={15} /> Onchain activity</span><span className="status review"><ShieldCheck size={14} /> Evidence-first</span></div><EmptyState icon={<BriefcaseBusiness size={28} />} title="Your ledger is ready" description="Connect your wallet to load jobs where you are the client or assigned worker." action={<Link className="button" href="/app/jobs/new">Create the first funded job</Link>} /></div></section></>;
}
