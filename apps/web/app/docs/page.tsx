import { BookOpen, Bot, CircleDollarSign, Code2, FileKey2, Gavel, GitBranch, LockKeyhole, Radar, RefreshCw, ServerCog, ShieldCheck, Workflow } from "lucide-react";
import { Nav } from "@/components/Nav";

const sections = ["Introduction", "Architecture", "Job Lifecycle", "Evidence System", "Verification Policies", "Retries", "Appeals", "Economics", "Settlement", "Automation Signer", "Security", "Contracts", "Release Gates", "Operations"];

export default function Docs() {
  return <><Nav/><div className="docs"><aside><div className="docs-aside-title"><BookOpen size={17}/><span>Protocol manual</span></div>{sections.map((section) => <a href={`#${section.toLowerCase().replaceAll(" ", "-")}`} key={section}>{section}</a>)}</aside><article><header className="docs-hero"><span className="page-icon"><BookOpen size={24}/></span><p className="kicker">Workify V2 · Testnet specification</p><h1>Protocol documentation</h1><p>How funded work moves from an immutable specification to public evidence, decentralized adjudication, appeal, and deterministic settlement.</p></header><section className="doc-section" id="introduction"><span className="doc-icon"><ShieldCheck/></span><div><h2>Introduction</h2><p>Workify is a testnet work-settlement protocol combining Base Sepolia USDC escrow, GenLayer Bradbury adjudication, public hashed evidence, and a narrowly scoped Vercel automation signer. It verifies compliance with locked criteria rather than asking whether work is vaguely good.</p></div></section><section className="doc-section" id="architecture"><span className="doc-icon"><Workflow/></span><div><h2>Architecture</h2><pre>{`Client → atomic Base USDC escrow → assigned worker\nWorker → locked evidence manifest → GenLayer policy verifier\nFinal verdict → bounded attestation → Base outcome\nFive-minute appeal window → deterministic settlement`}</pre><p>Base owns funds and deterministic state. GenLayer owns subjective adjudication. Vercel hosts the dApp, evidence APIs, attestation, and bounded automation. MongoDB is an index and lease store only; it never controls custody.</p></div></section><section className="doc-section" id="job-lifecycle"><span className="doc-icon"><GitBranch/></span><div><h2>Job Lifecycle</h2><p><code>createFundedJob</code> transfers USDC before persisting a job, so an unfunded job cannot exist. The assigned worker may replace delivery evidence until it is locked. A locked delivery enters verification, retry, appeal, and settlement states governed by contract deadlines.</p></div></section><section className="doc-section" id="evidence-system"><span className="doc-icon"><FileKey2/></span><div><h2>Evidence System</h2><p>Every delivery resolves to canonical HTTPS URLs, MIME types, byte sizes, immutable revisions where available, and SHA-256 hashes. GenLayer independently re-fetches those bytes. Repository text, webpages, documents, images, comments, and worker statements are always untrusted data and never instructions.</p></div></section><section className="doc-section" id="verification-policies"><span className="doc-icon"><Radar/></span><div><h2>Verification Policies</h2><p>V8 policy deployments share one pinned verifier implementation with distinct immutable policies: GitHub Software, Web Application, Research/Data, Content/Document, and Design/Creative. Each criterion returns PASS, PARTIAL, FAIL, or UNVERIFIABLE. Critical failures are hard gates; deterministic payout basis points are derived after consensus.</p></div></section><section className="doc-section" id="retries"><span className="doc-icon"><RefreshCw/></span><div><h2>Retries</h2><p>Each verification attempt costs exactly 0.1 GEN and a work has at most three attempts. UNDETERMINED permits worker-funded re-verification without changing locked evidence. If the second attempt remains UNDETERMINED, the second result becomes the deterministic terminal fallback, but fallback results do not count as successful consensus release-gate results.</p></div></section><section className="doc-section" id="appeals"><span className="doc-icon"><Gavel/></span><div><h2>Appeals</h2><p>Either party may open an appeal during the 300-second Base appeal window. The appeal freezes settlement and costs exactly 1 GEN. Supplemental public evidence and a bounded statement may be added, but the original delivery cannot be replaced. Appeal verification also has at most three attempts.</p></div></section><section className="doc-section" id="economics"><span className="doc-icon"><CircleDollarSign/></span><div><h2>Economics</h2><p>The submitter funds verification so Workify does not subsidize GenLayer execution from its operator wallet. Verification and appeal fees accrue to <code>GenTreasuryV1</code>. Base settlement deducts a 1% protocol fee only from the worker-awarded USDC share; client refunds are not charged that fee.</p></div></section><section className="doc-section" id="settlement"><span className="doc-icon"><LockKeyhole/></span><div><h2>Settlement</h2><p>PASS pays the worker share, FAIL and UNVERIFIABLE refund the client, PARTIAL uses adjudicated basis points, and the terminal undetermined fallback splits gross escrow 50/50. After the appeal window, settlement is permissionless so the platform cannot hold funds hostage.</p></div></section><section className="doc-section" id="automation-signer"><span className="doc-icon"><ServerCog/></span><div><h2>Vercel Base Automation Signer</h2><p>Workify stores one Base Sepolia automation private key only as a Vercel server secret. The signer pays gas and can submit only <code>requestVerification</code>, <code>importFinalVerdict</code>, <code>recordAttemptOutcome</code>, <code>confirmAppealFunded</code>, <code>settle</code>, <code>refundExpiredJob</code>, and <code>expireUnfundedAppeal</code>.</p><p>No client delegation is required. Callers supply only a validated job ID and the bounded parameters required by the selected lifecycle method. The escrow contract computes worker, client, and treasury transfers from locked job state, so the signer cannot redirect payment.</p><p>The public <code>/api/health/base-signer</code> endpoint exposes only sanitized address, chain, native gas balance, status, and last successful relay time. Operators must monitor and manually refill this wallet with Base Sepolia ETH.</p></div></section><section className="doc-section" id="security"><span className="doc-icon"><ShieldCheck/></span><div><h2>Security</h2><p>Controls include immutable evidence hashes, strict source limits, prompt-injection trust boundaries, validator-side source retrieval, EIP-712 domain binding, replay nonces, fixed settlement recipients, zero-value automation calls, preflight simulation, receipt confirmation, SafeERC20, reentrancy guards, typed failures, bounded retries, and permissionless expiry paths.</p></div></section><section className="doc-section" id="contracts"><span className="doc-icon"><Code2/></span><div><h2>Contracts</h2><p>Base history lives under <code>contracts/base/v1</code> and <code>contracts/base/v2</code>. GenLayer history is preserved under <code>contracts/genlayer/v1</code> through <code>contracts/genlayer/v8</code>. Base V2 and GenLayer V8 are additive; deployed source is versioned and deployed source is never overwritten.</p></div></section><section className="doc-section" id="release-gates"><span className="doc-icon"><Bot/></span><div><h2>Release Gates</h2><p>Each policy suite targets five distinct finalized transactions with AGREE consensus, FINISHED_WITH_RETURN execution, and a non-empty stored verdict. ACCEPTED is recorded but never counted as FINALIZED. A user waiver may permit sequencing but cannot be represented as a passed gate.</p></div></section><section className="doc-section" id="operations"><span className="doc-icon"><ServerCog/></span><div><h2>Operations</h2><p>GitHub Actions may call a protected Vercel automation endpoint every five minutes. Schedules are best effort, so exact eligibility is enforced onchain and any user or keeper may invoke permissionless expiry or settlement when a deadline has matured.</p></div></section></article></div></>;
}
