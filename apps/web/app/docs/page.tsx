import { Nav } from "@/components/Nav";
const sections=["Introduction","Architecture","Job Lifecycle","Evidence System","Verification","Retries","Appeals","Settlement","Fees","Security","Contracts","Operations"];
export default function Docs(){return <><Nav/><div className="docs"><aside>{sections.map(section=><a href={`#${section.toLowerCase().replaceAll(" ","-")}`} key={section}>{section}</a>)}</aside><article><h1>Workify Protocol Documentation</h1><p>Workify is a testnet work-settlement protocol combining Base Sepolia USDC escrow with GenLayer Bradbury adjudication and a narrowly scoped 1Shot public relayer settlement path.</p><h2 id="architecture">Architecture</h2><pre>{`Client → Base USDC Escrow → Worker Delivery\n       → Evidence Manifest → GenLayer Verdict\n       → 5-minute Appeal Window → Base Settlement`}</pre><h2 id="job-lifecycle">Job Lifecycle</h2><p>A job is created only after USDC transfer succeeds. The worker may replace evidence until locking delivery. Every locked delivery must be verified; client acceptance cannot bypass adjudication.</p><h2 id="evidence-system">Evidence System</h2><p>Users submit understandable public URLs. Workify resolves immutable revisions, canonical URLs, MIME types, sizes, and SHA-256 hashes. Onchain state stores bounded hashes rather than large artifacts.</p><h2 id="verification">Verification</h2><p>Five immutable policy deployments share one audited verifier implementation. Validators independently retrieve evidence and compare substantive decision fields, criteria outcomes, and bounded score tolerance.</p><h2 id="retries">Retries</h2><p>The worker prepays 0.1 GEN per attempt. There are three total attempts. Evidence remains locked. Three UNDETERMINED outcomes settle gross escrow 50/50.</p><h2 id="appeals">Appeals</h2><p>Either party may open one appeal within five minutes after verdict import. The appeal freezes payout and requires exactly 1 GEN within thirty minutes. Supplemental public evidence is allowed; original delivery remains immutable.</p><h2 id="settlement">Settlement</h2><p>PASS pays 100%, FAIL and UNVERIFIABLE refund the client, PARTIAL pays adjudicated basis points, and SPLIT pays 50%. A 1% fee is deducted only from the worker-awarded portion.</p><h2 id="security">Security</h2><p>Workify uses EIP-712 verdict attestations, nonce replay protection, immutable settlement recipients, pull-safe token transfers, permissionless expiry paths, canonical evidence hashes, typed errors, and bounded automation leases.</p><h2 id="contracts">Contracts</h2><p>Released source lives under <code>contracts/base/v1</code> and <code>contracts/genlayer/v1</code>. Future versions are additive and never overwrite deployed source.</p><h2 id="operations">Operations</h2><p>GitHub Actions calls a protected Vercel endpoint every five minutes. Automation eligibility is exact onchain, while execution may occur later because scheduled workflows are not real-time guarantees.</p></article></div></>}
