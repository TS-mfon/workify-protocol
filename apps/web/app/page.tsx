import Link from "next/link";
import { ArrowRight, Bot, CheckCircle2, GitPullRequest, Scale, ShieldCheck } from "lucide-react";
import { HeroMotion } from "@/components/HeroMotion";
import { Nav } from "@/components/Nav";

const features=[{icon:ShieldCheck,title:"Fund-first escrow",text:"A job cannot exist until its Base Sepolia USDC reward is atomically locked."},{icon:Bot,title:"Evidence-native verification",text:"Workify turns simple public URLs into pinned, hashed manifests evaluated on GenLayer."},{icon:Scale,title:"Appealable settlement",text:"Either party gets a five-minute appeal window before permissionless settlement."}];
export default function Home(){return <><HeroMotion/><Nav/><section className="hero"><div className="grid-bg"/><div className="shell" style={{position:"relative"}}><span className="eyebrow"><span className="pulse"/> Base Sepolia · GenLayer Bradbury</span><h1>Work gets paid when <span className="gradient">evidence proves it.</span></h1><p>Workify is a work settlement protocol combining USDC escrow, public evidence, decentralized AI adjudication, bounded appeals, and automated payout.</p><div className="actions"><Link className="button" href="/app/jobs/new">Create a funded job <ArrowRight size={16}/></Link><Link className="button secondary" href="/docs">Read protocol docs</Link></div><div className="stats"><div className="glass stat"><strong>5</strong><span>verification policies</span></div><div className="glass stat"><strong>3</strong><span>maximum attempts</span></div><div className="glass stat"><strong>5m</strong><span>appeal window</span></div><div className="glass stat"><strong>1%</strong><span>worker-award fee</span></div></div></div></section><section className="section shell" id="protocol"><span className="eyebrow"><GitPullRequest size={14}/> Protocol flow</span><h2>From specification to settlement.</h2><p className="muted">No subjective “looks good” approvals. Every verdict is tied to locked criteria and reproducible evidence.</p><div className="cards">{features.map(({icon:Icon,title,text})=><article className="glass card" key={title}><Icon color="#67e8f9"/><h3>{title}</h3><p>{text}</p></article>)}</div></section><section className="section shell" id="economics"><div className="glass card"><span className="eyebrow"><CheckCircle2 size={14}/> Deterministic economics</span><h2>Predictable outcomes, hard gates.</h2><p>PASS pays the worker, FAIL and UNVERIFIABLE refund the client, PARTIAL uses adjudicated basis points, and three UNDETERMINED attempts settle 50/50. Workify charges 1% only from the worker-awarded share.</p></div></section></>}
