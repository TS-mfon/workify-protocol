import type { Metadata } from "next";
import { Activity, Bot, CheckCircle2, ExternalLink, FileCode2, ShieldCheck } from "lucide-react";
import { Nav } from "@/components/Nav";
import { getResolvedCases } from "@/lib/explorer";

export const dynamic = "force-dynamic";
export const metadata: Metadata = { title: "Verdict Explorer", description: "Inspect Workify cases and contract-stored GenLayer verdicts." };

const short = (value: string) => `${value.slice(0, 8)}…${value.slice(-6)}`;

export default async function Explorer() {
  const cases = await getResolvedCases();
  const live = cases.filter((item) => item.verdict).length;
  return <><Nav /><main className="shell explorer-page"><section className="explorer-hero"><span className="eyebrow"><Activity size={14} /> GenLayer verdict explorer</span><h1>Resolved work, <span className="gradient">read from the contracts.</span></h1><p>Discovery comes from Workify’s finalized transaction index. Verdicts shown below are re-read from each deployed v7 verifier using <code>get_verdict</code>.</p><div className="stats explorer-stats"><div className="glass stat"><strong>{cases.length}</strong><span>finalized cases indexed</span></div><div className="glass stat"><strong>{live}</strong><span>live contract reads</span></div><div className="glass stat"><strong>5</strong><span>verification policies</span></div><div className="glass stat"><strong>Bradbury</strong><span>GenLayer network</span></div></div></section><section className="explorer-grid">{cases.map(({ policy, verifier, item, specification, verdict, readError }) => { const attempt = item.attempts.findLast((candidate) => candidate.status === "FINALIZED" && Boolean(candidate.verdict)) ?? item.attempts.at(-1); return <article className="glass explorer-card" key={`${verifier}:${item.jobId}`}><div className="explorer-card-head"><div><span className="kicker">{policy}</span><h2>{specification?.title || `Resolved case ${item.caseId}`}</h2></div>{verdict ? <span className={`verdict verdict-${verdict.decision.toLowerCase()}`}><CheckCircle2 size={14} /> {verdict.decision}</span> : <span className="verdict verdict-error">Read unavailable</span>}</div><p className="muted">{specification?.description || "Public Workify verification case."}</p><div className="explorer-meta"><span><Bot size={14} /> Contract {short(verifier)}</span><span><FileCode2 size={14} /> Job {short(item.jobId)}</span><span><ShieldCheck size={14} /> Attempt {attempt?.attempt ?? "—"}</span></div>{verdict ? <><div className="score-line"><div><span className="muted">Score</span><strong>{verdict.score}/100</strong></div><div><span className="muted">Worker payout</span><strong>{verdict.payout_bps / 100}%</strong></div><div><span className="muted">Criteria</span><strong>{verdict.criteria.length}</strong></div></div><div className="criteria-list">{verdict.criteria.map((criterion) => <div key={criterion.id}><span>{criterion.id} · {criterion.severity}</span><b className={`criterion-${criterion.decision.toLowerCase()}`}>{criterion.decision}</b></div>)}</div></> : <p className="explorer-error">{readError}</p>}<details><summary>Technical evidence</summary><dl className="technical-list"><div><dt>Transaction</dt><dd>{attempt?.transactionHash || "Unavailable"}</dd></div><div><dt>Result hash</dt><dd>{verdict?.result_hash || "Unavailable"}</dd></div><div><dt>Policy</dt><dd>{verdict?.policy_version || policy}</dd></div></dl><div className="explorer-links"><a href={item.specificationUrl} target="_blank" rel="noreferrer">Specification <ExternalLink size={13} /></a><a href={item.evidenceUrl} target="_blank" rel="noreferrer">Evidence <ExternalLink size={13} /></a></div></details></article>; })}</section></main></>;
}

